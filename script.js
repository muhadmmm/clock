const $=id=>document.getElementById(id);const pad=n=>String(n).padStart(2,'0');let format24=localStorage.getItem('clockFormat')!=='12';
function updateRadar(h,m,s,ms){const smoothS=s+ms/1000,smoothM=m+smoothS/60,smoothH=(h%12)+smoothM/60;[['hour-clock',smoothH,12,'hourValue',h%12||12],['minute-clock',smoothM,60,'minuteValue',m],['second-clock',smoothS,60,'secondValue',s]].forEach(([id,value,max,label,text])=>{const el=$(id);if(!el)return;const hand=el.querySelector('.radar-hand');if(hand)hand.style.transform=`translateX(-50%) rotate(${value/max*360}deg)`;const valueEl=$(label);if(valueEl)valueEl.textContent=pad(text)})}
function update(){const now=new Date(),h=now.getHours(),m=now.getMinutes(),s=now.getSeconds(),ms=now.getMilliseconds();const zoneEl=$('timezone');if(zoneEl)zoneEl.textContent=Intl.DateTimeFormat().resolvedOptions().timeZone;updateRadar(h,m,s,ms);const dateEl=$('date');if(dateEl)dateEl.textContent=`${now.getFullYear()}/${pad(now.getMonth()+1)}/${pad(now.getDate())}`;const dayEl=$('weekday');if(dayEl)dayEl.textContent=new Intl.DateTimeFormat('en-GB',{weekday:'long'}).format(now).toUpperCase().split('').join(' ');progress(now);requestAnimationFrame(update)}
function progress(now){const ys=new Date(now.getFullYear(),0,1),ye=new Date(now.getFullYear()+1,0,1);const value=Math.min(100,Math.max(0,(now-ys)/(ye-ys)*100));const bar=$('yearProgress'),pct=$('yearPct');if(bar)bar.style.width=`${value}%`;if(pct)pct.textContent=`${value.toFixed(1)}%`}
const quotes=['The people who are crazy enough to think they can change the world are the ones who do.','TIME IS A RESOURCE. USE IT.','PROGRESS > PERFECTION.','BUILD. BREAK. LEARN. REPEAT.'];let qi=0,ci=0;function type(){const q=quotes[qi],el=$('quoteText');if(el)el.textContent=q.slice(0,ci++);if(ci>q.length){setTimeout(()=>{ci=0;qi=(qi+1)%quotes.length;type()},2400);return}setTimeout(type,55)}type();
const canvas=$('matrix'),ctx=canvas?canvas.getContext('2d'):null;let drops=[],font=13,intensity=3;function resize(){if(!canvas)return;const r=canvas.getBoundingClientRect(),d=devicePixelRatio||1;canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0);drops=Array(Math.ceil(r.width/font)).fill(1)}window.addEventListener('resize',resize);resize();function matrix(){if(!canvas)return;const r=canvas.getBoundingClientRect();ctx.fillStyle='rgba(0,0,0,.16)';ctx.fillRect(0,0,r.width,r.height);ctx.font=`${font}px monospace`;for(let i=0;i<drops.length;i++){if(Math.random()>.16*intensity)continue;ctx.fillStyle=getComputedStyle(document.documentElement).getPropertyValue('--green');ctx.fillText(Math.random().toString(36).slice(2,3).toUpperCase(),i*font,drops[i]*font);if(drops[i]*font>r.height&&Math.random()>.95)drops[i]=0;drops[i]++}}setInterval(matrix,55);
const settings=$('settings'),settingsBtn=$('settingsBtn'),closeSettings=$('closeSettings');if(settingsBtn)settingsBtn.onclick=()=>settings.classList.add('open');if(closeSettings)closeSettings.onclick=()=>settings.classList.remove('open');if(settings)settings.onclick=e=>{if(e.target===settings)settings.classList.remove('open')};const range=$('matrixRange');if(range)range.oninput=e=>intensity=+e.target.value;const format=$('format');if(format){format.value=format24?'24':'12';format.onchange=e=>{format24=e.target.value==='24';localStorage.setItem('clockFormat',format24?'24':'12')}}const theme=$('themeBtn');if(theme)theme.onclick=()=>document.body.classList.toggle('amber');const fullscreen=$('fullscreenBtn');if(fullscreen)fullscreen.onclick=()=>document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen?.();update();